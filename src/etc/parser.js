
const categories = Object.create(null)



function walkCategories(type){
  if(categories[type]) return categories[type];

  let categorie = null;

  while (!categorie && next){
    categorie = 
  }



  return (categories[type] == categorie)
}

export default walkCategories;